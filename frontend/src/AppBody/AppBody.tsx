import styled from 'styled-components'
import { FC } from 'react'

const AppBodyConainer = styled.main`
  margin: 0 16px 16px;
`

export const AppBody: FC = () => {
  return <AppBodyConainer></AppBodyConainer>
}
